<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8" />
        <meta http-equiv="X-UA-Compatible" content="IE=edge" />
        <meta name="viewport" content="width=device-width, initial-scale=1.0" />
        <link
            href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.0-beta3/dist/css/bootstrap.min.css"
            rel="stylesheet"
            integrity="sha384-eOJMYsd53ii+scO/bJGFsiCZc+5NDVN2yr8+0RDqr0Ql0h+rP48ckxlpbzKgwra6"
            crossorigin="anonymous"
        />
        <title><%= title %></title>
    </head>
    <% if(notif) {%>
        <div class="alert alert-warning alert-dismissible fade show" role="alert">
            <%= notif %> 
            <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
        </div>
    <% } %>  
    <% if(title !== "Login") {%> 
    <nav class="navbar navbar-expand-lg navbar-light bg-light">
        <div class="container-fluid">
            <a class="navbar-brand" href="/">Recipe Book</a>
            <button
                class="navbar-toggler"
                type="button"
                data-bs-toggle="collapse"
                data-bs-target="#navbarSupportedContent"
                aria-controls="navbarSupportedContent"
                aria-expanded="false"
                aria-label="Toggle navigation"
            >
                <span class="navbar-toggler-icon"></span>
            </button>
        </div>
        <div class="collapse navbar-collapse" id="navbarSupportedContent">
            <ul class="navbar-nav me-auto mb-2 mb-lg-0">
                    <% if(title !== "Users") {%> 
                    <li class="nav-item">
                        <a class="nav-link <%= currentPage === 'Menus' ? 'active' : "" %>" href="/menus" <%= currentPage === 'Menus' ? `aria-current=page` : "" %>
                            >Menus</a
                        >
                    </li>
                    <li class="nav-item">
                        <a class="nav-link <%= currentPage === 'Ingredients' ? 'active' : "" %>" href="/ingredients" <%= currentPage === 'Ingredients' ? `aria-current=page` : "" %> >Ingredients</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link <%= currentPage === 'Suppliers' ? 'active' : "" %>" href="/suppliers" <%= currentPage === 'Suppliers' ? `aria-current=page` : "" %>>Supliers</a>
                    </li>
                    <% } %> 
                    <li class="nav-item">
                        <a class="nav-link" href="/users/logout">Logout</a>
                    </li>
                </ul>
            </div>
            
    </nav>
    <% } %> 
    <body style="width: 100vw">
</html>
